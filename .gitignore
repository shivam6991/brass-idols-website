# macOS
.DS_Store
._*

# Generated by build.py. Recreate any time with:  python3 build.py
# Kept out of the repo because it is a 1.9 MB copy of everything else, and it
# would conflict on every edit. Hosting serves index.html + assets/ directly.
site-single-file.html

# Editors
.vscode/
.idea/
*.swp
*~

# Node — test dependencies only, the site itself needs none
node_modules/
package-lock.json

# Python
__pycache__/
*.pyc
